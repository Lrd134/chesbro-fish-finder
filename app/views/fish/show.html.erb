<div class="fish box">
<%= render 'fish', locals: { fish: @fish, user: @user } %>
</div>
<% @fish.comments.each do | comment | %>
  <%= render partial: 'comments/comment', object: comment %><br>
<% end %>
