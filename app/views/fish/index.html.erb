

<h1>Fish</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fish.each do |fish| %>
      <tr>
        <td><%=link_to fish.title, fish_path(fish.slug, fish.category_slug)%></td>
        <td><%=fish.category.name.capitalize%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
