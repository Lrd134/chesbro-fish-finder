<div class="comment box">
<%= link_to "#{comment.user.display}", user_path(comment.user.id) %><br>
<%= link_to "Show ##{comment.id}", comment_path(comment.fish_slug, comment.category_slug, comment.id) %><br>
<%= comment.body %>
</div>