<%= link_to "Edit Comment", edit_comment_path(comment.fish_slug, comment.category_slug, comment.id) %>
<%= link_to "Destroy Comment", delete_fish_path(comment.fish_slug, comment.category_slug, comment.id), data: { confirm: "Are you sure you wish to delete your comment ##{comment.id}" }, method: :post %>
<%= link_to "Comment Creator", user_path(comment.user) %>
<%= comment.body %>
