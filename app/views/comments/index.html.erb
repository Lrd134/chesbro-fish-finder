<ul>
  <% @comments.each do | comment | %>
    <li><%=link_to "Comment ##{comment.id} by #{comment.user.username}", comments_path(comment.fish.slug, comment.fish.category.slug, comment.id)%></li>
  <% end %>
</ul>